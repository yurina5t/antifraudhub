
# 🛡️ AntifraudHub

**AntifraudHub** — сервис скоринга рисков и антифрода на основе ML-модели.  
Проект реализует полный цикл: **данные → модель → бизнес-решение → API → хранение → UI → Docker**.

---

## 🚀 Возможности

- 🔍 ML-скоринг фрода
- 🧠 3-зонная логика принятия решений: `ALLOW / REVIEW / BLOCK`
- 📦 Batch-прогон всех пользователей
- 👤 Скоринг одного пользователя
- 🗄️ Хранение предсказаний в Postgres
- ⚡ Источник данных — ClickHouse
- 📊 Swagger API
- 🐳 Docker / Docker Compose
- 🧪 Pytest (контракты + API)
- 🔐 Авторизация заложена архитектурно (временно отключена)

---

## 🧠 Антифрод-логика (3 зоны)

| Decision | Условие по `risk_score` | Значение |
|--------|--------------------------|----------|
| `ALLOW` | `< FRAUD_REVIEW_THRESHOLD` | Разрешить |
| `REVIEW` | `≥ FRAUD_REVIEW_THRESHOLD` | Ручная проверка |
| `BLOCK` | `≥ FRAUD_BLOCK_THRESHOLD` | Блокировка |

Пороговые значения задаются в конфигурации:

```python
# app/core/config.py
FRAUD_REVIEW_THRESHOLD = 0.134
FRAUD_BLOCK_THRESHOLD = 0.70
```
## 📡 API
🔹 Healthcheck

GET /api/fraud/health

🔹 Batch-предсказание (все пользователи)
POST /api/fraud/predict/batch

Query-параметры (опционально):
?decision=REVIEW&decision=BLOCK
Позволяет вернуть только пользователей на ручную проверку и блокировку.

🔹 Предсказание для одного пользователя
GET /api/fraud/predict/user/{user_email}
Пример ответа:
{
  "user_email": "user@example.com",
  "risk_score": 0.82,
  "decision": "BLOCK"
}

## 🧩 Масштабирование и архитектура
```
                ┌─────────────┐
                │   NGINX     │
                │ (Gateway)   │
                └──────┬──────┘
                       │
          ┌────────────┴────────────┐
          │                         │
┌─────────────────┐     ┌──────────────────┐
│ realtime-api    │     │ batch-worker     │
│ fast, low-lat   │     │ heavy, offline   │
│ WORKER_MODE=rt  │     │ WORKER_MODE=batch│
└─────────────────┘     └──────────────────┘
          │                         │
          └──────────┬──────────────┘
                     │
                PostgreSQL
```
Идея масштабирования

- **realtime-api** — обслуживает онлайн-запросы с минимальной задержкой
- **batch-worker** — выполняет тяжелые batch-прогоны
- Один Docker-образ, разные роли через WORKER_MODE
- Реалистичный production-паттерн для антифрод-систем

## 🗄️ Хранение данных

**ClickHouse**
- Источник фичей пользователей
- Используется для batch и single-user пайплайнов

**Postgres**
- Таблица predictions
- Хранит:
    - user_email
    - risk_score
    - decision


## 🧪 Тестирование

Используется **pytest**.

Покрыто:

- /health
- API контракт batch и single predict
- preprocess-контракт (FEATURES)

Запуск тестов:
PYTHONPATH=. pytest app/tests -v


## 🐳 Docker

**Сервисы**
- antifraud-api — FastAPI + ML
- postgres — хранение предсказаний

**Запуск**
docker compose up -d

Перезапуск API без пересборки
docker compose restart antifraud-api

## 🔐 Авторизация

- JWT-авторизация реализована архитектурно
- Используются:
    - dependencies/auth.py
    - dependencies/authz.py
    - auth/jwt_handler.py
- Временно отключена (INTERNAL_MODE = True)
- Все файлы сохранены для будущего включения
- Сервис сейчас работает как внутренний (internal).


## 🧩 Стек технологий

- Python 3.11
- FastAPI
- Pandas / NumPy
- ML-модель (AutoML)
- ClickHouse
- PostgreSQL
- SQLModel
- Docker / Docker Compose
- Pytest


## 🎯 Статус проекта

✅ Архитектура спроектирована  
✅ ML-логика реализована  
✅ REST API готов  
✅ Docker-окружение готово  
✅ Тесты добавлены  
🚧 Авторизация отключена (осознанно)  
